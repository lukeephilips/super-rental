
import { rentalPropertyTypeUppercase } from 'super-rentals/helpers/rental-property-type-uppercase';
import { module, test } from 'qunit';

module('Unit | Helper | rental property type uppercase');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rentalPropertyTypeUppercase([42]);
  assert.ok(result);
});

