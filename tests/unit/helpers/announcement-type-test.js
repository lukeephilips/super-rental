
import { announcementType } from 'super-rentals/helpers/announcement-type';
import { module, test } from 'qunit';

module('Unit | Helper | announcement type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = announcementType([42]);
  assert.ok(result);
});

